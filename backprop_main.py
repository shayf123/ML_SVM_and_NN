import backprop_dataimport backprop_networkimport matplotlib.pyplot as plt"""""""""training_data, test_data = backprop_data.load(train_size=10000, test_size=5000)net = skeleton_backprop_network.Network([784, 40, 10])net.SGD(training_data, epochs=30, mini_batch_size=10, learning_rate=0.1, test_data=test_data)training_data, test_data = backprop_data.load(train_size=50000,test_size=10000)net = skeleton_backprop_network.Network([784, 40, 10])net.SGD(training_data, epochs=30, mini_batch_size=10, learning_rate=0.1, test_data=test_data)"""""""""training_data, test_data = backprop_data.load(train_size=10000,test_size=5000)net = backprop_network.Network([784, 30, 30, 30, 30, 10])b_norm_lst = net.SGD(training_data, epochs=30, mini_batch_size=10000, learning_rate=0.1,test_data=test_data)epochs=30lst0 = [b_norm_lst[i][0] for i in range(epochs)]lst1 = [b_norm_lst[i][1] for i in range(epochs)]lst2 = [b_norm_lst[i][2] for i in range(epochs)]lst3 = [b_norm_lst[i][3] for i in range(epochs)]lst4 = [b_norm_lst[i][4] for i in range(epochs)]epochs_lst = [ep for ep in range(epochs)]plt.plot(epochs_lst,lst0, label='||d(l)/d(b0)||')plt.plot(epochs_lst,lst1, label='||d(l)/d(b1)||')plt.plot(epochs_lst,lst2, label='||d(l)/d(b2)||')plt.plot(epochs_lst,lst3, label='||d(l)/d(b3)||')plt.plot(epochs_lst,lst4, label='||d(l)/d(b4)||')plt.xlabel('epochs')plt.ylabel('norm')plt.legend()#plt.show()